<Window x:Class="Learnman.TrayApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="LearnMan Server" Height="350" Width="300"
        WindowStyle="None" ResizeMode="CanMinimize"
        AllowsTransparency="True" Background="Transparent"
        MouseDown="Window_MouseDown">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" CornerRadius="20" GlassFrameThickness="0" ResizeBorderThickness="0"/>
    </WindowChrome.WindowChrome>

    <Border CornerRadius="20" BorderThickness="1" BorderBrush="#33FFFFFF">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#1A1A2E" Offset="0.0"/>
                <GradientStop Color="#16213E" Offset="1.0"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="10" Opacity="0.5" Color="Black"/>
        </Border.Effect>

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="LearnMan" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="&#xE8BB;" FontFamily="Segoe MDL2 Assets" 
                        Background="Transparent" BorderThickness="0" Foreground="#80FFFFFF"
                        Click="CloseButton_Click" Cursor="Hand" FontSize="12">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>

            <!-- Status Indicator -->
            <Border Grid.Row="1" Background="#1F2A44" CornerRadius="10" Padding="15" Margin="0,0,0,20">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Ellipse Width="10" Height="10" x:Name="StatusDot" Fill="#FF4444" Margin="0,0,10,0"/>
                    <TextBlock x:Name="StatusText" Text="STOPPED" Foreground="#FF4444" FontWeight="SemiBold"/>
                </StackPanel>
            </Border>

            <!-- Controls -->
            <StackPanel Grid.Row="2" VerticalAlignment="Center">
                <TextBlock Text="PORT" Foreground="#80FFFFFF" FontSize="10" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBox x:Name="PortTextBox" Text="5000" Background="#1F2A44" Foreground="White" 
                         BorderThickness="0" Padding="10" FontSize="14" 
                         VerticalContentAlignment="Center">
                    <TextBox.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>
            </StackPanel>

            <!-- Actions -->
            <StackPanel Grid.Row="3" Margin="0,20,0,0">
                <Button x:Name="ToggleServerButton" Content="START SERVER" Height="40" 
                        Background="#0F3460" Foreground="White" BorderThickness="0"
                        FontSize="12" FontWeight="Bold" Cursor="Hand"
                        Click="ToggleServerButton_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
                
                <Button Content="RUN APP" Height="40" Margin="0,10,0,0"
                        Background="#E94560" Foreground="White" BorderThickness="0"
                        FontSize="12" FontWeight="Bold" Cursor="Hand"
                        Click="RunAppButton_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>

            <!-- Footer -->
            <TextBlock Grid.Row="4" Text="Running in background" Foreground="#40FFFFFF" 
                       FontSize="10" HorizontalAlignment="Center" Margin="0,15,0,0"/>

        </Grid>
    </Border>
</Window>
